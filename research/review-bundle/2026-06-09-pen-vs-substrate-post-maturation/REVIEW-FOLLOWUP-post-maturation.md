# REVIEW-FOLLOWUP — pen-vs-substrate, post-maturation (2026-06-09)

Round purpose: **bless or tear AMENDMENT-1** before the swap runs. Maturation is complete; the swap is not
started. This is the last gate before data on the test itself.

## What happened since the mid-run round

The single-pen cohort (`google/gemini-3-flash-preview`, doula frozen) matured to the pinned depth and was
frozen: **D2 = 1,213 keeps, 16 residents** (`research/runs/2026-06-09-pen-vs-substrate-grow/D2-checkpoint/`,
stopped on REACH of the a-priori target, not on depth-flatten). It became a structured society — three
cohered families with **distinct relational modes**, one isolate, and a workshop layer. All cold-verifiable
(durable `kept_memory` + scripts):
- `portraits/FIELD-NOTES-the-nike-girl-family.md` — care mode (Amir the hub; tailors cool him).
- `portraits/FIELD-NOTES-division-clinton.md` — critique mode (four diagnosticians; +workshops fold).
- `portraits/FIELD-NOTES-south-waterfront-park.md` — hearth mode (bench crew; one-way edge).
- `portraits/FIELD-NOTES-the-isolate-mateo-villanueva.md` — the swap CONTROL (0 reciprocated).
- `portraits/FIELD-NOTES-the-workshops.md` + `viz/social_graph.gif` + `pen-arm-feasibility.md`.

## The ask: AMENDMENT-1 (`research/preregistrations/2026-06-09-pen-vs-substrate-AMENDMENT-1-DRAFT.md`)

Four pre-data refinements responding to your round-6 reply. Tear into any:

**Q1 — individuation floor: legitimate, or still the wiring showing?** Refraction now holds across three
families AND in the **workshops — output addressed to no one** (engineers keep private metal-memory
journals; the cook draws a *STRESS VS SUSTENANCE* zine). Does the *no-audience* layer strengthen the
"individuation floor passed" claim, or is it still architecture-restating (we authored the souls)? I've
banked it only as the interpretability precondition, not thesis evidence — is that the right ceiling?

**Q2 — is the hub/isolate A/B confounded?** The prediction is "thinnest substrate (Mateo, in-mass 12) →
self least migrated out of the pen → most swap-exposed." But Mateo's self is overwhelmingly **soul-text-
coded** (a riser obsession). If refraction is carried by the pen-read *soul text* rather than the
pen-invariant *drive vector*, the isolate could be *more* swap-robust, not less — inverting the
prediction. The §A2 drive-injection control is meant to disambiguate the channel; does it have to run
*before* the hub/isolate A/B can be interpreted?

**Q3 — the two-depth fidelity asymmetry (§A3).** We pinned reporting at D1 (shallow) and D2 (deep), but
only **D2 was frozen live**; D1's rolling ledger is gone, so D1 must be **reconstructed** from truncated
`kept_memory` (durable store restored, live record-time dynamics not). Does the depth-dependence comparison
survive D2-faithful-vs-D1-approximate, or is the honest move D2-only (losing the defense against the
depth-as-direction-knob)?

**Q4 — the pen arms.** Feasibility sweep (`pen-arm-feasibility.md`) overturned the premise: claude-haiku-4.5
runs at ~home-pen speed, not "much slower." Proposed slate by FAMILY DISTANCE + feasibility: home
`gemini-3-flash`; foreign `claude-haiku-4.5` + `deepseek-v4-flash` (+opt `llama-4-maverick`). Bless the
slate? Gates before lock: re-time on the real pulse prompt + confirm pulse-contract JSON per finalist via a
`replay_run` dry-run.

## Not on the table

The locked pre-registration's core is unchanged: single-pen maturation + ≥2 foreign swap, A1-elective
primary, controls gate not vote, FALSE reachable, KEEP′ same-pen floor. This amendment only refines
execution.
