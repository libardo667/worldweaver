# Mr. Review — pre-launch PRE-MORTEM requested: "Pen vs Substrate"

This is a **new direction**, and it threads directly off your own desk. The convergence arc is
being retired by the operator — it was rigorous but the problem didn't yield much past "the model
owns the surface." This is the deeper, unanswered flip of that finding.

## Where it picks up on your desk
Your **open question #1** — *"Is there any runtime model without a monoculture prior?"* — is the
front half. The raw you already hold: swap only the runtime model on the same cold Portland with the
same diverse cast → theme, VOICE retention (0.92→0.73), and CONTACT (30%→0%) all move. **The pen owns
the surface.** Established.

The new question is the back half the desk never asked: **the pen owns the surface — does it also own
the SELF?** The project's load-bearing claim (`CLAUDE.md`) is that it does not: *"the self lives in
the soul + ledger + kept memory, not the model (the model is a swappable pen)."* Never tested.

## The one fact that makes it a fair test
The pen (`WW_INFERENCE_MODEL`, the pulse) and the affect substrate (`WW_EMBEDDING_MODEL`, the drive
vector) are **already different models** [`drive.py:1-19`, `cognitive_core.py:75`, `main.py:106`]. So
"swap the pen" holds the soul, the ledger, the kept memory, AND the drive vector all fixed, and varies
only the thing that turns those inputs into behavior. We hold the self-inputs constant and ask how
much of the self the pen determines.

## What I'm asking you for (BEFORE we lock or spend anything)
The draft pre-registration is in `DRAFT-preregistration-pen-vs-substrate.md`. Pre-mortem it:

1. **Predict the outcome.** Which verdict branch, and why — committed before the run, so the result
   can falsify *you* the way the venture-OFF null did.
2. **Attack the axis decomposition.** Is any axis I called "substrate-carried" (relationship fidelity,
   memory continuity) secretly **pen-carried** or **construction-fixed** like the drive vector? Is
   memory-continuity fatally trivial because the memory rides in the context window — or does the
   "theme shifts but memory holds" dissociation defuse that? Tell me where the decomposition leaks.
3. **Name the confound I missed.** I've pre-listed six (memory-in-context triviality, capability,
   population artifact, fork cleanliness, maturation/n, metaphysics overclaim). Find the seventh.
4. **Is the capability floor the right guard?** A weaker SWAP pen could degrade rather than "forget."
   Does a floor on act-validity + volume actually separate capability from self-loss, or does
   pen-capability contaminate the primaries in a way no floor catches?
5. **Pick the source of the matured self.** Two options, your call on which buys the cleaner read:
   - **fork a fresh, controlled shard** matured in-run (clean lineage, but costs maturation time and
     may be thinly individuated at fork);
   - **fork a live/historical shard** (a richer, deeper self to lose, but less controlled provenance).

The point of this round is to break the design *before* it runs. If you predict a null and we build
the protocol to falsify you, that's the deal again.
