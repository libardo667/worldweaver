# Core LLM provider configuration
# Required for live LLM-powered runtime: set at least one API key below.
OPENROUTER_API_KEY=
LLM_API_KEY=
OPENAI_API_KEY=
LLM_BASE_URL=https://openrouter.ai/api/v1
EMBEDDING_MODEL=openai/text-embedding-3-small

# Runtime tuning
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=2500
LLM_TIMEOUT_SECONDS=25
LLM_RETRIES=2

# Optional model used while generating a city pack.
LLM_CITY_BUILDER_MODEL=google/gemini-3-flash-preview

# Optional database override
WW_DB_PATH=worldweaver.db

# Legacy single-process shard identity. Generated shards keep these in shards/<node>/.env.
# CITY_ID names the portable city pack; SHARD_ID names this independently operated node.
CITY_ID=san_francisco
SHARD_ID=
SHARD_TYPE=city

# API cache behavior
STATE_MANAGER_CACHE_MAX_SIZE=500
STATE_MANAGER_CACHE_TTL_SECONDS=3600

# Runtime feature-flag defaults
WW_ENABLE_DEV_RESET=true

# Optional client-only overrides can go in `client/.env.local`.
# Compose sets VITE_PROXY_TARGET=http://backend:8000 for containerized client.
VITE_PROXY_TARGET=http://localhost:8000
