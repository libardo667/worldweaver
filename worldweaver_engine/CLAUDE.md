# Engine compatibility shim

Authoritative repository guidance is [`../CLAUDE.md`](../CLAUDE.md). Current direction and work items
live under [`../prune/`](../prune/); do not recreate the former `improvements/` path.

The canonical engine validation command is:

```bash
python scripts/dev.py check
```
